[project]
name = "auth"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.136.1",
    "uvicorn>=0.47.0",
    "livekit-api>=1.1.0",
    "pydantic-settings>=2.14.1",
    "common",
]

[tool.uv.sources]
common = { workspace = true }
