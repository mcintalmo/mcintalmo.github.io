[project]
name = "portfolio-mcp"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "fastmcp>=0.1.0",
    "pyyaml>=6.0.0",
    "common",
]

[tool.uv.sources]
common = { workspace = true }
