[project]
name = "portfolio-mcp"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "common",
    "fastmcp>=3.4.2",
    "pyyaml>=6.0.3",
]

[tool.uv.sources]
common = { workspace = true }
