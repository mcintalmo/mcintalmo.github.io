[project]
name = "portfolio-mcp"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "fastmcp>=3.3.1",
    "pyyaml>=6.0.3",
    "common",
]

[tool.uv.sources]
common = { workspace = true }
